package cPAddonsAvailable::Concrete;
our %list = (
    'Concrete::CMS::concrete5' => {
        'VERSION' => '0.7',    # $Vendor::Category::Name::VERSION
        'version' => '5.5.1',    # script's version, same as $meta_info->{'version'}
    },

    # repeat for each cPAddon you want to distribute
);
1;
